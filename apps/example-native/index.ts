/**
 * Expo's entry point. `registerRootComponent` is `AppRegistry.registerComponent`
 * plus the bookkeeping that makes the app work in Expo Go as well as a build.
 */
import { registerRootComponent } from "expo";
import App from "./App";

registerRootComponent(App);
