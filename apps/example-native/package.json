{
  "name": "example-native",
  "private": true,
  "//": "Run by hand, never by CI. `check` is a no-op on purpose: this app exists to be looked at on a device, and there is nothing here a headless runner could assert that `packages/harness` does not already assert better.",
  "main": "index.ts",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "check": "true"
  },
  "dependencies": {
    "@blobatar/react-native": "workspace:*",
    "blobatar": "workspace:*",
    "expo": "~54.0.0",
    "expo-status-bar": "~3.0.0",
    "react": "19.1.0",
    "react-native": "0.81.4",
    "react-native-svg": "^15.15.5"
  },
  "devDependencies": {
    "@types/react": "^19",
    "typescript": "~5.9.2"
  },
  "//typescript": "Pinned to 5.x here, against the workspace's TypeScript 7. Expo's CLI reads the project tsconfig through the TypeScript API and crashes on 7 before Metro ever starts, which is the same incompatibility ADR-0010 records breaking `@sveltejs/package`. Local to this app so the rest of the workspace stays on 7."
}
